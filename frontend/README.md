# Frontend Folder - PŘIPRAVENO PRO BUDOUCÍ ROZŠÍŘENÍ

## 📋 Účel
Tato složka je připravena pro budoucí frontend framework (React, Vue.js, Angular)

## ⚠️ AKTUÁLNÍ STAV
**Frontend aplikace JIŽ FUNGUJE!** Soubory jsou umístěny ve `static/` složce:
- ✅ `static/js/main.js` (400 řádků)
- ✅ `static/js/modules/calculator/calculations.js` (300 řádků)
- ✅ `static/css/main.css` (700 řádků)
- ✅ `templates/index.html` (400 řádků)

## 🎯 Budoucí použití
Tato složka je připravena pro:
1. **Single Page Application (SPA)** - React/Vue.js komponenty
2. **TypeScript** - Typovaný JavaScript
3. **Modern Build Tools** - Webpack, Vite
4. **Component-based architecture** - Znovupoužitelné komponenty

## 📁 Struktura složek
```
frontend/
├── components/     # Znovupoužitelné UI komponenty
├── modules/        # Funkční moduly (calculator, chatbot, atd.)
├── services/       # API komunikace
└── utils/          # Pomocné funkce
```

## 💡 Migrace z static/
Pokud budete chtít přejít na moderní framework:
1. Nainstalujte React/Vue.js: `npm init vite@latest`
2. Zkopírujte logiku z `static/js/` do této složky
3. Rozdělte do komponent
4. Nastavte build proces

## 🚀 Pro OKAMŽITÉ použití
Použijte stávající soubory ve `static/` - plně funkční! ✅
